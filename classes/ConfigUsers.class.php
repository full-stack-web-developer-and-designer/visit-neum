<?php
/*
class ConfigUsers {
	public static function get($paths = null){
		if($paths){
		$conf = $GLOBALS['ConfigUsers'];
		$paths = explode("/", $paths);
		foreach($paths as $bit){
			if(isset($conf[$bit])){
				$conf = $conf[$bit];
			}
		}
		return $conf;
		}
		return false;
	}
}
*/